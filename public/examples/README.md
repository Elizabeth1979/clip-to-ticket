# Example Videos

Place your example video file here for development and demonstration purposes.

## Recommended filename:
- `Ed-WixUser-edited.mp4` (or `.webm`, `.mov`)

## Usage:
The example video will be accessible at `/examples/Ed-WixUser-edited.mp4` and can be loaded via the "Load Example" button in the UI.

## Note:
If your video file is large (>50MB), consider adding it to `.gitignore` to avoid committing it to version control.
